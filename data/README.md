# Data directory

`grocery_list.txt` is a small sample used by the categorizer example.

PDF files placed in this directory are ignored by Git. The PDF RAG application uses an upload control, so no PDF needs to be committed to the repository.
