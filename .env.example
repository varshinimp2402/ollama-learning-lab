# Ollama server. For a local installation, the default is usually correct.
OLLAMA_HOST=http://localhost:11434

# Chat model used by the examples and RAG app.
OLLAMA_MODEL=llama3.2

# Embedding model used by the PDF RAG app.
OLLAMA_EMBED_MODEL=nomic-embed-text
